---
phase: 09-attractions
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - triply/src/app/features/attraction-search/attraction-search.component.ts
  - triply/src/app/features/attraction-search/attraction-search.component.html
  - triply/src/app/features/attraction-search/attraction-search.component.scss
autonomous: true

must_haves:
  truths:
    - "User can enter a city name and receive a list of tourist attractions for that city"
    - "Each attraction displays an official link when one is available"
    - "Clicking Add to Itinerary adds the attraction to TripStateService and it appears in the itinerary view"
    - "Attractions without official links do not show a broken or placeholder link button"
    - "Each attraction card shows the category as a chip"
  artifacts:
    - path: "triply/src/app/features/attraction-search/attraction-search.component.ts"
      provides: "AttractionSearchComponent with city search form, signal state, add to itinerary"
      exports: ["AttractionSearchComponent"]
    - path: "triply/src/app/features/attraction-search/attraction-search.component.html"
      provides: "Template with search form, result cards with nullable link, category chip"
      contains: "attraction-card"
    - path: "triply/src/app/features/attraction-search/attraction-search.component.scss"
      provides: "Styles following tour-search pattern with attraction-specific adjustments"
      contains: "attraction-card"
  key_links:
    - from: "triply/src/app/features/attraction-search/attraction-search.component.ts"
      to: "triply/src/app/core/api/attraction-api.service.ts"
      via: "inject(AttractionApiService)"
      pattern: "inject\\(AttractionApiService\\)"
    - from: "triply/src/app/features/attraction-search/attraction-search.component.ts"
      to: "triply/src/app/core/services/trip-state.service.ts"
      via: "inject(TripStateService).addAttraction()"
      pattern: "addAttraction"
    - from: "triply/src/app/features/attraction-search/attraction-search.component.html"
      to: "Attraction model link field"
      via: "@if (attraction.link !== null) conditional rendering"
      pattern: "attraction\\.link !== null"
---

<objective>
Create the AttractionSearchComponent with city search form, result cards displaying category and nullable official link, and add-to-itinerary functionality.

Purpose: Final UI component for Phase 9, completing the attractions feature. This is the last search category (6 of 6) and follows the established pattern from Phases 4-8, with the unique addition of nullable link handling and category chip display.

Output: Three component files (attraction-search.component.ts, .html, .scss) providing a fully functional attraction search experience.
</objective>

<execution_context>
@C:/Users/Pichau/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Pichau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Pattern references — read these before coding
@triply/src/app/features/tour-search/tour-search.component.ts
@triply/src/app/features/tour-search/tour-search.component.html
@triply/src/app/features/tour-search/tour-search.component.scss
@triply/src/app/core/models/trip.models.ts
@triply/src/app/core/models/base.model.ts
@triply/src/app/core/services/trip-state.service.ts

# Created in 09-01
@triply/src/app/core/api/attraction-api.service.ts
@triply/src/app/core/api/attraction.mapper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AttractionSearchComponent TypeScript</name>
  <files>
    triply/src/app/features/attraction-search/attraction-search.component.ts
  </files>
  <action>
Create AttractionSearchComponent following TourSearchComponent pattern exactly, with these differences:

1. Component metadata:
   - selector: 'app-attraction-search'
   - standalone: true
   - imports: [MATERIAL_IMPORTS, ReactiveFormsModule, CommonModule]
   - templateUrl: './attraction-search.component.html'
   - styleUrl: './attraction-search.component.scss'

2. Injected services:
   - `private readonly attractionApi = inject(AttractionApiService)`
   - `private readonly tripState = inject(TripStateService)`
   - `private readonly snackBar = inject(MatSnackBar)`

3. Form: single field (same as tours, simpler than hotels/transport):
   - `attractionSearchForm = new FormGroup({ city: new FormControl('', Validators.required) })`

4. Signal state:
   - `searchResults = signal<Attraction[]>([])`
   - `isSearching = signal(false)`
   - `hasSearched = signal(false)`

5. NO sorting computed signal (attractions have no price). Display results in API order.

6. `searchAttractions(): void` method:
   - Guard: if form invalid, return
   - Extract city from form value with nullish coalescing: `const city = this.attractionSearchForm.value.city ?? ''`
   - Set isSearching(true), hasSearched(true)
   - Call `this.attractionApi.searchAttractions({ city })` with pipe(finalize(() => this.isSearching.set(false)))
   - On next: if result.error, show snackbar 'Failed to search attractions. Please try again.', set results to []. Otherwise set results to result.data.

7. `addToItinerary(attraction: Attraction): void` method:
   - Call `this.tripState.addAttraction({ ...attraction, addedToItinerary: true })`
   - Show snackbar 'Attraction added to itinerary'

8. NO formatDuration helper (attractions don't have duration).

Import from: '@angular/core' (Component, signal, computed, inject), '@angular/common' (CommonModule), '@angular/forms' (FormControl, FormGroup, Validators, ReactiveFormsModule), '@angular/material/snack-bar' (MatSnackBar), 'rxjs/operators' (finalize), MATERIAL_IMPORTS from core, AttractionApiService from core/api, TripStateService from core/services, Attraction from core/models.
  </action>
  <verify>
Run `npx tsc --noEmit` from triply/ — zero errors.
Confirm: AttractionSearchComponent is exported.
Confirm: inject(AttractionApiService) and inject(TripStateService) are present.
Confirm: addToItinerary calls tripState.addAttraction() (not addActivity).
Confirm: searchAttractions subscribes to attractionApi.searchAttractions().
  </verify>
  <done>
AttractionSearchComponent has city-only reactive form, calls AttractionApiService.searchAttractions(), and adds results to itinerary via TripStateService.addAttraction().
  </done>
</task>

<task type="auto">
  <name>Task 2: Attraction search template and styles</name>
  <files>
    triply/src/app/features/attraction-search/attraction-search.component.html
    triply/src/app/features/attraction-search/attraction-search.component.scss
  </files>
  <action>
**attraction-search.component.html** — Follow tour-search.component.html closely with these differences:

1. View header:
   ```html
   <div class="view-header">
     <h1>Search Attractions</h1>
     <p>Discover tourist attractions and points of interest in any city</p>
   </div>
   ```

2. Search form card — same as tours but field name is "city":
   - mat-form-field with mat-label "City"
   - input with formControlName="city" placeholder="e.g. Paris"
   - mat-icon matPrefix: "place"
   - mat-error for required validation
   - Submit button with @if(isSearching()) pattern (two buttons: disabled with spinner, or enabled with search icon)
   - Button text: "Search Attractions"

3. Results section — similar to tours but NO price column, WITH category chip, WITH nullable link:
   ```html
   @if (searchResults().length > 0) {
     <div class="results-section">
       <div class="results-header">
         <h2>{{ searchResults().length }} attraction{{ searchResults().length !== 1 ? 's' : '' }} found</h2>
       </div>

       @for (attraction of searchResults(); track attraction.id) {
         <mat-card class="attraction-card">
           <mat-card-content>
             <div class="attraction-header">
               <div class="attraction-info">
                 <h3 class="attraction-name">{{ attraction.name }}</h3>
                 <div class="attraction-meta">
                   <mat-chip>{{ attraction.category }}</mat-chip>
                   <span class="separator">-</span>
                   <mat-icon>place</mat-icon>
                   <span>{{ attraction.city }}</span>
                 </div>
               </div>
             </div>
             @if (attraction.description) {
               <p class="attraction-description">{{ attraction.description }}</p>
             }
           </mat-card-content>

           <mat-card-actions>
             <button mat-button color="primary" (click)="addToItinerary(attraction)">
               <mat-icon>add</mat-icon>
               Add to Itinerary
             </button>

             @if (attraction.link !== null) {
               <a mat-button [href]="attraction.link.url" target="_blank" rel="noopener noreferrer">
                 <mat-icon>open_in_new</mat-icon>
                 View Official Site
               </a>
             }
           </mat-card-actions>
         </mat-card>
       }
     </div>
   }
   ```
   KEY DIFFERENCES from tour template:
   - No price column (attractions are free informational listings)
   - Category displayed as `<mat-chip>` (not present in tours)
   - Link is CONDITIONALLY rendered with `@if (attraction.link !== null)` — tours always show link
   - No duration display (attractions don't have duration)
   - Results iterated directly from searchResults() (no sortedResults computed)

4. Empty state — same pattern as tours:
   ```html
   @if (searchResults().length === 0 && !isSearching() && hasSearched()) {
     <mat-card class="empty-state">
       <mat-card-content>
         <mat-icon>museum</mat-icon>
         <p>No attractions found. Try a different city.</p>
       </mat-card-content>
     </mat-card>
   }
   ```
   Use `museum` icon matching the nav link.

**attraction-search.component.scss** — Copy tour-search.component.scss, then rename all .tour-* classes to .attraction-*:

- .tour-card -> .attraction-card
- .tour-header -> .attraction-header
- .tour-info -> .attraction-info
- .tour-name -> .attraction-name
- .tour-meta -> .attraction-meta
- .tour-description -> .attraction-description

REMOVE the .tour-price block entirely (no price in attractions).

ADD mat-chip styling in .attraction-meta:
```scss
.attraction-meta {
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--mat-sys-on-surface-variant);

  mat-icon {
    font-size: 18px;
    height: 18px;
    width: 18px;
  }

  mat-chip {
    font-size: 0.75rem;
  }

  .separator {
    color: var(--mat-sys-outline);
  }
}
```

Keep all other sections identical to tour-search: .view-header, .search-form-card, .form-row, .search-button, .results-section, .results-header, .empty-state, and the responsive @media query. The .attraction-header does NOT need the price-related flex layout since there's no price — it can be simplified to just .attraction-info without the space-between.
  </action>
  <verify>
Run `npx tsc --noEmit` from triply/ — zero errors.
Confirm: Template uses @if/@for control flow (NOT *ngIf/*ngFor).
Confirm: `@if (attraction.link !== null)` guards the official link rendering.
Confirm: `<mat-chip>{{ attraction.category }}</mat-chip>` is present.
Confirm: `addToItinerary(attraction)` is called on button click.
Confirm: Empty state uses museum icon.
Confirm: SCSS has .attraction-card, .attraction-header, .attraction-info, .attraction-name, .attraction-meta, .attraction-description classes.
Confirm: No .tour-* or price-related CSS classes remain.
  </verify>
  <done>
Attraction search template renders result cards with category chip, conditional official link (only when not null), add-to-itinerary button, description, and empty state. Styles follow tour-search pattern renamed to attraction-* with price section removed and mat-chip styling added.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `ng serve` compiles without warnings (test by building)
3. AttractionSearchComponent renders city search form
4. Result cards show: name, category chip, city, conditional description, conditional official link
5. "Add to Itinerary" button calls TripStateService.addAttraction()
6. Official link only renders when attraction.link !== null
7. Empty state shows when no results after search
8. Responsive layout works on mobile (single column)
</verification>

<success_criteria>
- User can enter a city name and submit the form to search for attractions
- Search results display as cards with name, category chip, city, optional description
- Each card has "Add to Itinerary" button that calls TripStateService.addAttraction()
- Official link button ("View Official Site") appears ONLY when attraction.link is not null
- Empty state message appears when search returns no results
- Styles are consistent with tour-search component (same spacing, layout, design tokens)
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-attractions/09-02-SUMMARY.md`
</output>
