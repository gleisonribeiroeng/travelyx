---
phase: 08-tours-experiences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triply/src/app/core/api/tour-api.service.ts
  - triply/src/app/core/api/tour.mapper.ts
  - triply/src/app/app.routes.ts
  - triply/src/app/core/components/header/header.component.html
autonomous: true

must_haves:
  truths:
    - "TourApiService can call Viator /partner/products/search and return ApiResult<Activity[]>"
    - "TourMapper transforms Viator product response to canonical Activity model"
    - "/tours route is registered in app.routes.ts"
    - "Tours nav link appears in the header navigation"
  artifacts:
    - path: "triply/src/app/core/api/tour-api.service.ts"
      provides: "Tour search API service extending BaseApiService"
      exports: ["TourApiService", "TourSearchParams"]
    - path: "triply/src/app/core/api/tour.mapper.ts"
      provides: "Viator product to Activity model transformation"
      exports: ["TourMapper", "ViatorProduct", "TourSearchParams"]
  key_links:
    - from: "triply/src/app/core/api/tour-api.service.ts"
      to: "BaseApiService"
      via: "extends BaseApiService, super('tours')"
      pattern: "super\\('tours'\\)"
    - from: "triply/src/app/core/api/tour-api.service.ts"
      to: "triply/src/app/core/api/tour.mapper.ts"
      via: "inject(TourMapper)"
      pattern: "inject\\(TourMapper\\)"
    - from: "triply/src/app/app.routes.ts"
      to: "triply/src/app/features/tour-search/tour-search.component.ts"
      via: "lazy loadComponent import"
      pattern: "path:\\s*'tours'"
---

<objective>
Create TourApiService and TourMapper for Viator Partner API integration, plus register the /tours route and add a Tours link to the header navigation.

Purpose: Establishes the API layer for tours/experiences search (Phase 8 Wave 1), enabling the search UI in Plan 08-02 to call searchTours() and receive Activity[] results.

Output: tour-api.service.ts, tour.mapper.ts, updated app.routes.ts with /tours route, updated header with Tours nav link.
</objective>

<execution_context>
@C:/Users/Pichau/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Pichau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing patterns to follow exactly
@triply/src/app/core/api/transport-api.service.ts
@triply/src/app/core/api/transport.mapper.ts
@triply/src/app/core/api/base-api.service.ts
@triply/src/app/core/models/trip.models.ts
@triply/src/app/core/models/base.model.ts
@triply/src/app/core/api/interceptors/api-key.interceptor.ts
@triply/src/app/app.routes.ts
@triply/src/app/core/components/header/header.component.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: TourMapper and TourApiService</name>
  <files>
    triply/src/app/core/api/tour.mapper.ts
    triply/src/app/core/api/tour-api.service.ts
  </files>
  <action>
Create tour.mapper.ts following the TransportMapper two-parameter pattern (does NOT implement Mapper interface):

1. Define ViatorProduct interface with all optional fields:
   - productCode?: string
   - title?: string
   - description?: string
   - images?: Array<{ variants: Array<{ url: string }> }>
   - pricing?: { summary?: { fromPrice?: number; fromPriceBeforeDiscount?: number }; currency?: string }
   - bookingInfo?: { bookingUrl?: string }
   - location?: { address?: string; latitude?: number; longitude?: number }
   - duration?: { fixedDurationInMinutes?: number }

2. Define TourSearchParams interface:
   - destination: string (free text city name)

3. Create @Injectable({ providedIn: 'root' }) TourMapper class with mapResponse(raw: ViatorProduct, params: TourSearchParams): Activity method:
   - id: String(raw.productCode || crypto.randomUUID())
   - source: 'tours'
   - addedToItinerary: false
   - name: raw.title || 'Untitled Tour'
   - description: raw.description || ''
   - location: { latitude: raw.location?.latitude || 0, longitude: raw.location?.longitude || 0 }
   - city: params.destination (fallback to search destination)
   - durationMinutes: raw.duration?.fixedDurationInMinutes ?? null (IMPORTANT: use ?? null, NOT || null — zero is a valid duration)
   - price: { total: raw.pricing?.summary?.fromPrice || 0, currency: raw.pricing?.currency || 'USD' }
   - link: { url: raw.bookingInfo?.bookingUrl || '#', provider: 'Viator' }

Create tour-api.service.ts following TransportApiService pattern exactly:

1. @Injectable({ providedIn: 'root' }) TourApiService extends BaseApiService
2. Constructor: super('tours') — maps to /api/tours proxy -> https://api.viator.com
3. inject(TourMapper) as private readonly mapper
4. searchTours(params: TourSearchParams): Observable<ApiResult<Activity[]>> method:
   - Uses this.post (NOT get) because Viator /products/search is a POST endpoint
   - Endpoint path: '/partner/products/search'
   - Request body:
     ```
     {
       filtering: { destination: params.destination },
       pagination: { offset: 0, limit: 20 },
       sorting: { sort: 'REVIEW_AVG_RATING_D' }
     }
     ```
   - Pipe: withBackoff()
   - Map response: defensive extraction with response.products || response.data?.products || response.data || []
   - Map each product through this.mapper.mapResponse(product, params)
   - Return ApiResult<Activity[]> with { data: mapped, error: null }
   - catchError returns { data: [], error } (AppError type)
5. Re-export TourSearchParams: `export type { TourSearchParams } from './tour.mapper';`
6. Add JSDoc note that API endpoint is HYPOTHETICAL (Viator requires partnership application), same pattern as TransportApiService.

CRITICAL: Tours is NOT a RapidAPI source. It uses standard X-API-Key header via apiKeyInterceptor. The 'tours' source is already in RAPID_API_SOURCES exclusion (it's not in the array), so the interceptor correctly uses X-API-Key. No interceptor changes needed.
  </action>
  <verify>
Run: npx tsc --noEmit --project triply/tsconfig.json
Expected: Zero type errors.
Verify tour.mapper.ts exports TourMapper, ViatorProduct, TourSearchParams.
Verify tour-api.service.ts exports TourApiService and re-exports TourSearchParams.
Verify TourApiService constructor calls super('tours').
Verify searchTours uses this.post (not this.get) for POST endpoint.
Verify durationMinutes uses ?? null (not || null).
  </verify>
  <done>
TourMapper transforms ViatorProduct to Activity with safe fallbacks for all fields including null-safe duration.
TourApiService extends BaseApiService('tours'), calls POST /partner/products/search with withBackoff() and per-source catchError returning ApiResult<Activity[]>.
  </done>
</task>

<task type="auto">
  <name>Task 2: /tours route and Tours nav link</name>
  <files>
    triply/src/app/app.routes.ts
    triply/src/app/core/components/header/header.component.html
  </files>
  <action>
Update app.routes.ts:
1. Add lazy-loaded /tours route BEFORE the wildcard ** route (after /transport route):
   ```typescript
   {
     path: 'tours',
     loadComponent: () =>
       import('./features/tour-search/tour-search.component').then(m => m.TourSearchComponent),
   },
   ```
2. Follow exact pattern of existing /transport route entry.

Update header.component.html:
1. Add Tours nav link between the Transport link and the Itinerary link:
   ```html
   <a mat-button routerLink="/tours" routerLinkActive="active-link">
     <mat-icon>local_activity</mat-icon>
     Tours
   </a>
   ```
2. Use 'local_activity' icon (Material icon for tours/events/experiences).
3. Position: after Transport link, before Itinerary link (consistent with phase order).
  </action>
  <verify>
Run: npx tsc --noEmit --project triply/tsconfig.json
Expected: Zero type errors (lazy import will resolve when component exists in 08-02).
Verify app.routes.ts contains path: 'tours' entry before wildcard.
Verify header.component.html contains routerLink="/tours" with local_activity icon.
  </verify>
  <done>
/tours route registered with lazy loading pointing to TourSearchComponent (created in 08-02).
Tours navigation link visible in header with local_activity icon, positioned between Transport and Itinerary.
  </done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit --project triply/tsconfig.json — zero type errors
2. tour.mapper.ts exports: TourMapper class, ViatorProduct interface, TourSearchParams interface
3. tour-api.service.ts exports: TourApiService class, re-exports TourSearchParams type
4. TourApiService constructor calls super('tours')
5. searchTours uses this.post() (POST request, not GET)
6. TourMapper.mapResponse correctly handles null duration (durationMinutes: raw.duration?.fixedDurationInMinutes ?? null)
7. app.routes.ts has /tours path before ** wildcard
8. header.component.html has Tours link with local_activity icon between Transport and Itinerary
</verification>

<success_criteria>
- TourApiService and TourMapper compile with zero type errors
- API service uses POST for /partner/products/search endpoint
- Mapper safely handles all optional ViatorProduct fields with fallback chains
- durationMinutes correctly produces null for missing/undefined duration (not 0)
- /tours route registered for lazy loading
- Tours navigation link visible in header
</success_criteria>

<output>
After completion, create `.planning/phases/08-tours-experiences/08-01-SUMMARY.md`
</output>
