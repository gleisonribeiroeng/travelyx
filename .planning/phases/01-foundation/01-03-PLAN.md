---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - triply/src/app/app.component.ts
  - triply/src/app/app.component.html
  - triply/src/app/app.component.scss
  - triply/src/app/core/components/header/header.component.ts
  - triply/src/app/core/components/header/header.component.html
  - triply/src/app/core/components/header/header.component.scss
  - triply/src/app/core/components/footer/footer.component.ts
  - triply/src/app/core/components/footer/footer.component.html
  - triply/src/app/core/components/footer/footer.component.scss
  - triply/src/app/features/search/search.component.ts
  - triply/src/app/features/search/search.component.html
  - triply/src/app/features/search/search.component.scss
  - triply/src/app/features/itinerary/itinerary.component.ts
  - triply/src/app/features/itinerary/itinerary.component.html
  - triply/src/app/features/itinerary/itinerary.component.scss
autonomous: true

must_haves:
  truths:
    - "User sees a header with the app name 'Triply' and navigation links for Search and Itinerary"
    - "User can click 'Search' nav link and be navigated to /search view"
    - "User can click 'Itinerary' nav link and be navigated to /itinerary view"
    - "The active navigation link is visually distinguished from inactive links"
    - "User sees a footer with copyright text at the bottom of the page"
    - "The layout uses the Angular Material theme colors and design tokens consistently"
  artifacts:
    - path: "triply/src/app/core/components/header/header.component.ts"
      provides: "Application header with toolbar and navigation links"
      exports: ["HeaderComponent"]
    - path: "triply/src/app/core/components/footer/footer.component.ts"
      provides: "Application footer with copyright"
      exports: ["FooterComponent"]
    - path: "triply/src/app/app.component.html"
      provides: "Shell layout composing header, router-outlet, and footer"
      contains: "app-header"
  key_links:
    - from: "triply/src/app/core/components/header/header.component.html"
      to: "@angular/router"
      via: "routerLink directives on navigation links"
      pattern: "routerLink.*/(search|itinerary)"
    - from: "triply/src/app/core/components/header/header.component.html"
      to: "@angular/router"
      via: "routerLinkActive directive for active state styling"
      pattern: "routerLinkActive"
    - from: "triply/src/app/app.component.html"
      to: "triply/src/app/core/components/header/header.component.ts"
      via: "app-header selector in template"
      pattern: "<app-header"
    - from: "triply/src/app/app.component.html"
      to: "triply/src/app/core/components/footer/footer.component.ts"
      via: "app-footer selector in template"
      pattern: "<app-footer"
---

<objective>
Build the layout shell — a header with navigation, a main content area with router-outlet, and a footer — using Angular Material components and the design tokens established in Plan 01-02. Enhance the placeholder feature components to look like real views.

Purpose: Gives the app its visual structure and proves end-to-end navigation works with Material styling. This is the user-facing proof that the foundation is complete.
Output: A navigable app with a themed header (Triply branding + Search/Itinerary nav links), content area, and footer.
</objective>

<execution_context>
@C:/Users/Pichau/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Pichau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create header and footer layout components</name>
  <files>
    triply/src/app/core/components/header/header.component.ts
    triply/src/app/core/components/header/header.component.html
    triply/src/app/core/components/header/header.component.scss
    triply/src/app/core/components/footer/footer.component.ts
    triply/src/app/core/components/footer/footer.component.html
    triply/src/app/core/components/footer/footer.component.scss
  </files>
  <action>
Generate the header and footer components using Angular CLI:

```bash
cd C:/Users/Pichau/triply
npx ng generate component core/components/header --standalone --skip-tests
npx ng generate component core/components/footer --standalone --skip-tests
```

**HeaderComponent (`header.component.ts`):**
- Import `RouterLink`, `RouterLinkActive` from `@angular/router`
- Import `MatToolbarModule` from `@angular/material/toolbar`
- Import `MatButtonModule` from `@angular/material/button`
- Import `MatIconModule` from `@angular/material/icon`
- Selector: `app-header`
- Add all above to the component's `imports` array

**header.component.html:**
```html
<mat-toolbar>
  <span class="app-name">Triply</span>
  <span class="spacer"></span>
  <nav class="nav-links">
    <a mat-button routerLink="/search" routerLinkActive="active-link">
      <mat-icon>search</mat-icon>
      Search
    </a>
    <a mat-button routerLink="/itinerary" routerLinkActive="active-link">
      <mat-icon>map</mat-icon>
      Itinerary
    </a>
  </nav>
</mat-toolbar>
```

**header.component.scss:**
```scss
mat-toolbar {
  background-color: var(--mat-sys-primary);
  color: var(--mat-sys-on-primary);
}

.app-name {
  font-size: 1.25rem;
  font-weight: 600;
  letter-spacing: 0.5px;
}

.spacer {
  flex: 1 1 auto;
}

.nav-links a {
  color: var(--mat-sys-on-primary);
  text-transform: uppercase;
  font-size: 0.875rem;
  letter-spacing: 0.5px;

  mat-icon {
    margin-right: 4px;
    font-size: 18px;
    height: 18px;
    width: 18px;
  }
}

.active-link {
  background-color: rgba(255, 255, 255, 0.15);
  border-radius: var(--triply-border-radius-sm);
}
```

**FooterComponent (`footer.component.ts`):**
- Selector: `app-footer`
- No special imports needed beyond basic Angular

**footer.component.html:**
```html
<footer class="app-footer">
  <p>&copy; 2026 Triply — Plan your perfect trip</p>
</footer>
```

**footer.component.scss:**
```scss
.app-footer {
  text-align: center;
  padding: var(--triply-spacing-md);
  color: var(--mat-sys-on-surface-variant);
  font-size: 0.875rem;
  border-top: 1px solid var(--mat-sys-outline-variant);
}
```

Build to verify compilation:
```bash
npx ng build
```
  </action>
  <verify>
Run from `triply/`:
- `npx ng build` completes with zero errors
- `core/components/header/header.component.ts` exists, is standalone, imports RouterLink, RouterLinkActive, MatToolbarModule, MatButtonModule, MatIconModule
- `core/components/footer/footer.component.ts` exists and is standalone
- Header template uses `routerLink` and `routerLinkActive` directives
  </verify>
  <done>Header component with Material toolbar, Triply branding, Search/Itinerary nav links with routerLinkActive, and footer component with copyright text — both standalone, themed with --mat-sys-* tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble shell layout and enhance feature placeholders</name>
  <files>
    triply/src/app/app.component.ts
    triply/src/app/app.component.html
    triply/src/app/app.component.scss
    triply/src/app/features/search/search.component.ts
    triply/src/app/features/search/search.component.html
    triply/src/app/features/search/search.component.scss
    triply/src/app/features/itinerary/itinerary.component.ts
    triply/src/app/features/itinerary/itinerary.component.html
    triply/src/app/features/itinerary/itinerary.component.scss
  </files>
  <action>
**Update AppComponent** to compose the shell layout:

**app.component.ts** — Add imports for HeaderComponent, FooterComponent, and RouterOutlet:
```typescript
import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';
import { HeaderComponent } from './core/components/header/header.component';
import { FooterComponent } from './core/components/footer/footer.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [RouterOutlet, HeaderComponent, FooterComponent],
  templateUrl: './app.component.html',
  styleUrl: './app.component.scss',
})
export class AppComponent {}
```

**app.component.html:**
```html
<app-header></app-header>
<main class="main-content">
  <router-outlet></router-outlet>
</main>
<app-footer></app-footer>
```

**app.component.scss:**
```scss
:host {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.main-content {
  flex: 1;
  padding: var(--triply-spacing-lg);
  max-width: var(--triply-max-content-width);
  width: 100%;
  margin: 0 auto;
  box-sizing: border-box;
}
```

**Enhance feature placeholders** to look like real views using Material components:

**search.component.ts** — Import `MatCardModule`, `MatIconModule` from Material:
```typescript
import { Component } from '@angular/core';
import { MatCardModule } from '@angular/material/card';
import { MatIconModule } from '@angular/material/icon';

@Component({
  selector: 'app-search',
  standalone: true,
  imports: [MatCardModule, MatIconModule],
  templateUrl: './search.component.html',
  styleUrl: './search.component.scss',
})
export class SearchComponent {}
```

**search.component.html:**
```html
<div class="view-header">
  <h1>Search</h1>
  <p>Search for flights, hotels, car rentals, transport, tours, and attractions.</p>
</div>
<mat-card class="placeholder-card">
  <mat-card-content>
    <mat-icon class="placeholder-icon">flight</mat-icon>
    <p>Search features will be available in upcoming phases.</p>
  </mat-card-content>
</mat-card>
```

**search.component.scss:**
```scss
.view-header {
  margin-bottom: var(--triply-spacing-lg);

  h1 {
    margin: 0 0 var(--triply-spacing-sm) 0;
    color: var(--mat-sys-on-surface);
  }

  p {
    color: var(--mat-sys-on-surface-variant);
    margin: 0;
  }
}

.placeholder-card {
  text-align: center;
  padding: var(--triply-spacing-xl);

  .placeholder-icon {
    font-size: 48px;
    height: 48px;
    width: 48px;
    color: var(--mat-sys-primary);
    margin-bottom: var(--triply-spacing-md);
  }
}
```

**itinerary.component.ts** — Import `MatCardModule`, `MatIconModule`:
```typescript
import { Component } from '@angular/core';
import { MatCardModule } from '@angular/material/card';
import { MatIconModule } from '@angular/material/icon';

@Component({
  selector: 'app-itinerary',
  standalone: true,
  imports: [MatCardModule, MatIconModule],
  templateUrl: './itinerary.component.html',
  styleUrl: './itinerary.component.scss',
})
export class ItineraryComponent {}
```

**itinerary.component.html:**
```html
<div class="view-header">
  <h1>Itinerary</h1>
  <p>Your trip itinerary will appear here, organized by day and time.</p>
</div>
<mat-card class="placeholder-card">
  <mat-card-content>
    <mat-icon class="placeholder-icon">map</mat-icon>
    <p>Add items from Search to start building your itinerary.</p>
  </mat-card-content>
</mat-card>
```

**itinerary.component.scss:**
```scss
.view-header {
  margin-bottom: var(--triply-spacing-lg);

  h1 {
    margin: 0 0 var(--triply-spacing-sm) 0;
    color: var(--mat-sys-on-surface);
  }

  p {
    color: var(--mat-sys-on-surface-variant);
    margin: 0;
  }
}

.placeholder-card {
  text-align: center;
  padding: var(--triply-spacing-xl);

  .placeholder-icon {
    font-size: 48px;
    height: 48px;
    width: 48px;
    color: var(--mat-sys-primary);
    margin-bottom: var(--triply-spacing-md);
  }
}
```

Build and serve to verify the full layout:
```bash
npx ng build
```
  </action>
  <verify>
Run from `triply/`:
- `npx ng build` completes with zero errors
- `app.component.html` contains `<app-header>`, `<router-outlet>`, and `<app-footer>`
- Serve with `npx ng serve` and verify:
  1. Header shows "Triply" with Search and Itinerary navigation links
  2. Clicking "Search" navigates to /search, shows search placeholder with Material card
  3. Clicking "Itinerary" navigates to /itinerary, shows itinerary placeholder with Material card
  4. Active nav link has visual highlight (active-link class)
  5. Footer shows copyright at bottom
  6. Layout uses primary theme color in header
  </verify>
  <done>Complete shell layout assembled: themed header with navigation, main content area with router-outlet, footer. Feature placeholders use Material cards with consistent design tokens. Navigation between /search and /itinerary works with active state indication.</done>
</task>

</tasks>

<verification>
1. `npx ng build` in `triply/` completes with zero errors
2. Header renders with Material toolbar using primary theme color
3. "Search" and "Itinerary" nav links work — clicking each navigates to the correct route
4. Active nav link has the `active-link` CSS class applied (visible background change)
5. Footer renders at the bottom with copyright text
6. Search view shows a Material card placeholder with flight icon
7. Itinerary view shows a Material card placeholder with map icon
8. All custom spacing uses `--triply-spacing-*` tokens
9. Colors reference `--mat-sys-*` design tokens
</verification>

<success_criteria>
- Header with Triply branding and Search/Itinerary navigation
- Active link visually distinguished via routerLinkActive
- Footer with copyright at page bottom
- Shell layout uses flexbox for sticky footer
- Feature placeholders themed with Material cards and icons
- All styling uses design tokens (--mat-sys-* and --triply-*)
- Full navigation flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
