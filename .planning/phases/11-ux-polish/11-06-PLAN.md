---
phase: 11-ux-polish
plan: 06
type: execute
wave: 4
depends_on: ["11-03", "11-04", "11-05"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "The full application builds without errors after all UX polish changes"
    - "All 6 search components have error banner integration, mat-raised-button for primary action, and responsive layout"
    - "The header navigation is mobile-responsive and the itinerary view renders correctly on mobile"
  artifacts: []
  key_links: []
---

<objective>
Run a final build and verification pass confirming all Phase 11 UX polish requirements are met across the entire application.

Purpose: After 5 parallel plan tracks modify files across the codebase, this plan verifies everything integrates correctly and the build is clean.
Output: Clean build confirmation and verification that all 7 UX requirements are satisfied.
</objective>

<execution_context>
@C:/Users/Pichau/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Pichau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ux-polish/11-RESEARCH.md
@.planning/phases/11-ux-polish/11-03-SUMMARY.md
@.planning/phases/11-ux-polish/11-04-SUMMARY.md
@.planning/phases/11-ux-polish/11-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build verification and UX requirements audit</name>
  <files></files>
  <action>
Run a full production build and audit all UX requirements:

**Step 1: Build**
```bash
cd triply && npx ng build
```
Must complete with zero errors and zero warnings related to Phase 11 changes.

**Step 2: Verify UX-01 (Loading states)**
Grep all 6 search component HTML files for `isSearching()` and `mat-spinner`. All must have the spinner-in-button pattern.
- search.component.html: isSearching() + mat-spinner
- hotel-search.component.html: isSearching() + mat-spinner
- car-search.component.html: isSearching() + mat-spinner
- transport-search.component.html: isSearching() + mat-spinner
- tour-search.component.html: isSearching() + mat-spinner
- attraction-search.component.html: isSearching() + mat-spinner

**Step 3: Verify UX-02 (Empty states)**
Grep all 6 search component HTML files for `hasSearched()` and `empty-state`. All must have the three-condition guard (length === 0 && !isSearching() && hasSearched()). Also verify itinerary.component.html has empty state.

**Step 4: Verify UX-03 (Per-source error banners)**
Grep all 6 search component HTML files for `app-error-banner`. All must contain the error banner element.

**Step 5: Verify UX-04 (Mobile responsive)**
Grep all 6 search component SCSS files + header.component.scss + itinerary.component.scss for `@media`. All must have responsive breakpoints.

**Step 6: Verify UX-05 (Consistent card design)**
Visual audit: All 6 search components use mat-card for result cards with consistent spacing patterns (margin-bottom var(--triply-spacing-md), padding var(--triply-spacing-md) in card content).

**Step 7: Verify UX-06 (Navigation reachable)**
Verify header.component.html has all 7 routerLink entries and the mobile menu toggle.

**Step 8: Verify UX-07 (Primary action prominence)**
Grep all 6 search component HTML files for `mat-raised-button`. The "Add to Itinerary" button in each must be mat-raised-button color="primary". External links must remain mat-button.

If any requirement is NOT met, fix it directly. Log what was found and fixed.
  </action>
  <verify>Full production build succeeds. All 7 UX requirements confirmed by grep across all component files.</verify>
  <done>The application builds cleanly and all 7 UX requirements (UX-01 through UX-07) are verified as satisfied across all components.</done>
</task>

</tasks>

<verification>
1. `npx ng build` succeeds with zero errors
2. UX-01: All 6 search components have isSearching() + mat-spinner loading pattern
3. UX-02: All 6 search components have three-condition empty state guard + itinerary empty state
4. UX-03: All 6 search components have `<app-error-banner>` element
5. UX-04: All component SCSS files have @media responsive breakpoints
6. UX-05: All 6 search components use consistent mat-card with design token spacing
7. UX-06: Header has 7 nav links + mobile hamburger menu
8. UX-07: All 6 search components use mat-raised-button for "Add to Itinerary"
</verification>

<success_criteria>
Production build is clean and all 7 UX requirements (UX-01 through UX-07) are verified as met across the entire application.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-polish/11-06-SUMMARY.md`
</output>
