---
phase: 11-ux-polish
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - triply/src/app/features/search/search.component.ts
  - triply/src/app/features/search/search.component.html
  - triply/src/app/features/search/search.component.scss
  - triply/src/app/features/hotel-search/hotel-search.component.ts
  - triply/src/app/features/hotel-search/hotel-search.component.html
  - triply/src/app/features/hotel-search/hotel-search.component.scss
  - triply/src/app/features/car-search/car-search.component.ts
  - triply/src/app/features/car-search/car-search.component.html
  - triply/src/app/features/car-search/car-search.component.scss
autonomous: true

must_haves:
  truths:
    - "When Flights API fails, a persistent error banner with source name and message appears in the flights search page — not a transient snackbar"
    - "When Hotels API fails, a persistent error banner appears in the hotels search page"
    - "When Car Rental API fails, a persistent error banner appears in the car rental search page"
    - "All three components use mat-raised-button color='primary' for 'Add to Itinerary' action"
  artifacts:
    - path: "triply/src/app/features/search/search.component.ts"
      provides: "Flight search with error signal and ErrorBannerComponent integration"
      contains: "errorMessage"
    - path: "triply/src/app/features/hotel-search/hotel-search.component.ts"
      provides: "Hotel search with error signal and ErrorBannerComponent integration"
      contains: "errorMessage"
    - path: "triply/src/app/features/car-search/car-search.component.ts"
      provides: "Car search with error signal and ErrorBannerComponent integration"
      contains: "errorMessage"
  key_links:
    - from: "triply/src/app/features/search/search.component.html"
      to: "triply/src/app/shared/components/error-banner/error-banner.component.ts"
      via: "template usage of <app-error-banner>"
      pattern: "app-error-banner"
    - from: "triply/src/app/features/hotel-search/hotel-search.component.html"
      to: "triply/src/app/shared/components/error-banner/error-banner.component.ts"
      via: "template usage of <app-error-banner>"
      pattern: "app-error-banner"
    - from: "triply/src/app/features/car-search/car-search.component.html"
      to: "triply/src/app/shared/components/error-banner/error-banner.component.ts"
      via: "template usage of <app-error-banner>"
      pattern: "app-error-banner"
---

<objective>
Wire per-source error banners, upgrade button prominence, and apply consistent card styling to the Flights, Hotels, and Cars search components.

Purpose: These 3 components (the first half of the 6 search categories) get error banners replacing transient snackbars, mat-raised-button for primary actions, and the shared result-card class for visual consistency.
Output: Updated TS, HTML, and SCSS for search, hotel-search, and car-search components.
</objective>

<execution_context>
@C:/Users/Pichau/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Pichau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ux-polish/11-RESEARCH.md
@.planning/phases/11-ux-polish/11-01-SUMMARY.md
@.planning/phases/11-ux-polish/11-02-SUMMARY.md
@triply/src/app/features/search/search.component.ts
@triply/src/app/features/search/search.component.html
@triply/src/app/features/search/search.component.scss
@triply/src/app/features/hotel-search/hotel-search.component.ts
@triply/src/app/features/hotel-search/hotel-search.component.html
@triply/src/app/features/hotel-search/hotel-search.component.scss
@triply/src/app/features/car-search/car-search.component.ts
@triply/src/app/features/car-search/car-search.component.html
@triply/src/app/features/car-search/car-search.component.scss
@triply/src/app/shared/components/error-banner/error-banner.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error banners and button prominence to Flights search</name>
  <files>
    triply/src/app/features/search/search.component.ts
    triply/src/app/features/search/search.component.html
    triply/src/app/features/search/search.component.scss
  </files>
  <action>
**search.component.ts changes:**
1. Import ErrorBannerComponent from '../../shared/components/error-banner/error-banner.component'
2. Add ErrorBannerComponent to the imports array
3. Add two new signals: `errorMessage = signal<string | null>(null)` and `errorSource = signal<string | null>(null)`
4. Add dismissError() method: sets both errorMessage and errorSource to null
5. In searchFlights() method:
   - Add `this.errorMessage.set(null)` at the start (clear previous errors)
   - In the subscribe next handler, when result.error is truthy: set `this.errorMessage.set(result.error.message)` and `this.errorSource.set(result.error.source)` instead of showing snackbar
   - Remove the snackbar.open call from the error path (keep only the itinerary success snackbar)
   - Remove the error: () => {} handler entirely (withFallback already catches)

**search.component.html changes:**
1. Add error banner BEFORE the results section (after the search form card, before @if (searchResults())):
```html
@if (errorMessage()) {
  <app-error-banner
    [source]="errorSource() ?? 'Flights'"
    [message]="errorMessage()!"
    (dismiss)="dismissError()"
  />
}
```
2. Change ALL `mat-button` to `mat-raised-button` on the "Add to Itinerary" buttons in the flight result cards. Specifically change:
   `<button mat-button color="primary" (click)="addToItinerary(flight)">`
   to:
   `<button mat-raised-button color="primary" (click)="addToItinerary(flight)">`
3. Keep the external link as `mat-button` (text-only, secondary action)

**search.component.scss changes:**
- Remove the duplicate `.empty-state` block (already covered by global styles in 11-02)
- The card-specific styles (.flight-card, .flight-header, etc.) should remain since flights have a unique layout
- No other SCSS changes needed (flights already has full mobile responsive styles)
  </action>
  <verify>Run `npx ng build --configuration=development 2>&1 | head -30` from triply directory. Build succeeds. Grep search.component.html for "app-error-banner" and "mat-raised-button".</verify>
  <done>Flight search shows persistent error banner on API failure (not snackbar), "Add to Itinerary" uses mat-raised-button color="primary".</done>
</task>

<task type="auto">
  <name>Task 2: Add error banners and button prominence to Hotels and Cars search</name>
  <files>
    triply/src/app/features/hotel-search/hotel-search.component.ts
    triply/src/app/features/hotel-search/hotel-search.component.html
    triply/src/app/features/hotel-search/hotel-search.component.scss
    triply/src/app/features/car-search/car-search.component.ts
    triply/src/app/features/car-search/car-search.component.html
    triply/src/app/features/car-search/car-search.component.scss
  </files>
  <action>
Apply the SAME error banner pattern from Task 1 to hotel-search and car-search components:

**For EACH component (hotel-search and car-search):**

**TS file changes:**
1. Import ErrorBannerComponent from '../../shared/components/error-banner/error-banner.component'
2. Add ErrorBannerComponent to the imports array
3. Add `errorMessage = signal<string | null>(null)` and `errorSource = signal<string | null>(null)` signals
4. Add `dismissError()` method setting both to null
5. In the search method (searchHotels / searchCars):
   - Add `this.errorMessage.set(null)` at the start
   - In subscribe next, when result.error: set errorMessage/errorSource from result.error instead of snackbar
   - Remove snackbar.open from error path (keep only itinerary success snackbar)

**HTML file changes:**
1. Add error banner block after search form card, before results section:
```html
@if (errorMessage()) {
  <app-error-banner
    [source]="errorSource() ?? 'Hotels'"
    [message]="errorMessage()!"
    (dismiss)="dismissError()"
  />
}
```
(Use 'Cars' for car-search component)

2. Change `mat-button` to `mat-raised-button` on "Add to Itinerary" buttons:
   - hotel-search: `<button mat-button color="primary" (click)="addToItinerary(hotel)">` → `<button mat-raised-button color="primary" (click)="addToItinerary(hotel)">`
   - car-search: `<button mat-button color="primary" (click)="addToItinerary(car)">` → `<button mat-raised-button color="primary" (click)="addToItinerary(car)">`

**SCSS files:** No changes needed — both already have full mobile responsive styles.
  </action>
  <verify>Run `npx ng build --configuration=development 2>&1 | head -30`. Build succeeds. Grep hotel-search.component.html and car-search.component.html for "app-error-banner" and "mat-raised-button".</verify>
  <done>Hotels and Cars search pages show persistent error banners on API failure and use mat-raised-button for primary action.</done>
</task>

</tasks>

<verification>
1. `npx ng build --configuration=development` completes without errors
2. All 3 components (search, hotel-search, car-search) have errorMessage and errorSource signals
3. All 3 HTML templates contain `<app-error-banner>` element
4. All 3 HTML templates use `mat-raised-button` for "Add to Itinerary" buttons
5. No snackbar.open calls remain for error handling in these 3 components (only success snackbars for itinerary add)
</verification>

<success_criteria>
Flights, Hotels, and Cars search pages display per-source error banners on API failure (not transient snackbars), and all "Add to Itinerary" buttons use mat-raised-button color="primary" for visual prominence.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-polish/11-03-SUMMARY.md`
</output>
