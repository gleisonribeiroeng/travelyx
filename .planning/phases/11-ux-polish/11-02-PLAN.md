---
phase: 11-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - triply/src/styles.scss
autonomous: true

must_haves:
  truths:
    - "A shared SCSS mixin named search-result-card exists in styles.scss that defines consistent card spacing, header layout, price display, and mobile breakpoint"
    - "Any component can @use the mixin to get consistent card styling without duplicating CSS"
  artifacts:
    - path: "triply/src/styles.scss"
      provides: "Shared search-result-card mixin and empty-state mixin"
      contains: "search-result-card"
  key_links:
    - from: "triply/src/styles.scss"
      to: "component SCSS files"
      via: "@use and @include"
      pattern: "mixin search-result-card"
---

<objective>
Create shared SCSS mixins in styles.scss that define the unified card design for all 6 search categories, ensuring visual consistency when applied in later plans.

Purpose: A single source of truth for card styling eliminates duplication across 6 component SCSS files and guarantees visual consistency. Must exist before plans 11-03 and 11-04 apply it.
Output: Updated triply/src/styles.scss with two new mixins: search-result-card and empty-state-card.
</objective>

<execution_context>
@C:/Users/Pichau/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Pichau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ux-polish/11-RESEARCH.md
@triply/src/styles.scss
@triply/src/app/features/tour-search/tour-search.component.scss
@triply/src/app/features/hotel-search/hotel-search.component.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared SCSS mixins to styles.scss</name>
  <files>triply/src/styles.scss</files>
  <action>
Add two SCSS mixins to the end of triply/src/styles.scss (after the existing body block). These are global mixins that component SCSS files will NOT @use (Angular component styles are scoped). Instead, they will be applied as global utility classes that components can reference.

Actually, Angular component styles are encapsulated. The correct approach for sharing styles across components is to define them as global styles. Add the following AFTER the existing body block:

**Mixin 1: .search-result-card (global class)**
Define as a global CSS class that any component can apply to its mat-card elements. Since Angular Material cards are custom elements, global styles can target them via ::ng-deep or by adding classes that match global selectors.

Better approach: Since Angular component styles are encapsulated, we should NOT use mixins (they require @use imports which don't work cross-component in Angular). Instead, define global utility styles:

```scss
// Shared search result card styles
// Components apply .result-card class to get consistent card design
.result-card {
  margin-bottom: var(--triply-spacing-md);

  .result-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: var(--triply-spacing-md);
  }

  .result-info {
    flex: 1;
  }

  .result-name {
    font-size: 1.1rem;
    font-weight: 500;
    margin: 0 0 var(--triply-spacing-sm) 0;
    color: var(--mat-sys-on-surface);
  }

  .result-meta {
    display: flex;
    align-items: center;
    gap: 6px;
    color: var(--mat-sys-on-surface-variant);

    mat-icon {
      font-size: 18px;
      height: 18px;
      width: 18px;
    }

    .separator {
      color: var(--mat-sys-outline);
    }
  }

  .result-price {
    text-align: right;

    .amount {
      font-size: 1.4rem;
      font-weight: bold;
      color: var(--mat-sys-primary);
    }

    .price-label {
      font-size: 0.8rem;
      color: var(--mat-sys-on-surface-variant);
    }
  }

  .result-details {
    display: flex;
    flex-direction: column;
    gap: var(--triply-spacing-sm);
    margin-top: var(--triply-spacing-sm);

    .detail-row {
      display: flex;
      align-items: center;
      gap: 4px;
      color: var(--mat-sys-on-surface-variant);

      mat-icon {
        font-size: 18px;
        height: 18px;
        width: 18px;
      }

      span {
        font-size: 0.9rem;
      }
    }
  }

  @media (max-width: 600px) {
    .result-header {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--triply-spacing-sm);
    }
  }
}

// Shared empty state card styles
.empty-state-card {
  text-align: center;
  padding: var(--triply-spacing-xl);
  margin-top: var(--triply-spacing-xl);

  mat-card-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--triply-spacing-md);
  }

  .empty-icon {
    font-size: 48px;
    height: 48px;
    width: 48px;
    color: var(--mat-sys-on-surface-variant);
  }

  p {
    color: var(--mat-sys-on-surface-variant);
    margin: 0;
  }
}
```

IMPORTANT: Do NOT remove or modify any existing content in styles.scss. Only APPEND after the existing body block. The mat.theme() include and :root tokens must remain untouched.
  </action>
  <verify>
Run `npx ng build --configuration=development 2>&1 | head -20` from the triply directory. Build should succeed. Verify the new classes exist in styles.scss by searching for "result-card" and "empty-state-card".
  </verify>
  <done>Global SCSS classes .result-card and .empty-state-card are defined in styles.scss, providing a single source of truth for card styling that plans 11-03 and 11-04 will apply to individual components.</done>
</task>

</tasks>

<verification>
1. `npx ng build --configuration=development` completes without errors
2. styles.scss contains .result-card with .result-header, .result-info, .result-price, .result-meta, .result-details sub-classes
3. styles.scss contains .empty-state-card with centered icon and message layout
4. Both classes include @media (max-width: 600px) responsive breakpoints
5. Existing mat.theme() and :root tokens are preserved unchanged
</verification>

<success_criteria>
Shared global CSS classes for result cards and empty states exist in styles.scss and the build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-polish/11-02-SUMMARY.md`
</output>
