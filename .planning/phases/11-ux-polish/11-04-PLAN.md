---
phase: 11-ux-polish
plan: 04
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - triply/src/app/features/transport-search/transport-search.component.ts
  - triply/src/app/features/transport-search/transport-search.component.html
  - triply/src/app/features/transport-search/transport-search.component.scss
  - triply/src/app/features/tour-search/tour-search.component.ts
  - triply/src/app/features/tour-search/tour-search.component.html
  - triply/src/app/features/tour-search/tour-search.component.scss
  - triply/src/app/features/attraction-search/attraction-search.component.ts
  - triply/src/app/features/attraction-search/attraction-search.component.html
  - triply/src/app/features/attraction-search/attraction-search.component.scss
autonomous: true

must_haves:
  truths:
    - "When Transport API fails, a persistent error banner appears in the transport search page"
    - "When Tours API fails, a persistent error banner appears in the tours search page"
    - "When Attractions API fails, a persistent error banner appears in the attractions search page"
    - "All three components use mat-raised-button color='primary' for 'Add to Itinerary' action"
  artifacts:
    - path: "triply/src/app/features/transport-search/transport-search.component.ts"
      provides: "Transport search with error signal and ErrorBannerComponent"
      contains: "errorMessage"
    - path: "triply/src/app/features/tour-search/tour-search.component.ts"
      provides: "Tour search with error signal and ErrorBannerComponent"
      contains: "errorMessage"
    - path: "triply/src/app/features/attraction-search/attraction-search.component.ts"
      provides: "Attraction search with error signal and ErrorBannerComponent"
      contains: "errorMessage"
  key_links:
    - from: "triply/src/app/features/transport-search/transport-search.component.html"
      to: "triply/src/app/shared/components/error-banner/error-banner.component.ts"
      via: "template usage of <app-error-banner>"
      pattern: "app-error-banner"
    - from: "triply/src/app/features/tour-search/tour-search.component.html"
      to: "triply/src/app/shared/components/error-banner/error-banner.component.ts"
      via: "template usage of <app-error-banner>"
      pattern: "app-error-banner"
    - from: "triply/src/app/features/attraction-search/attraction-search.component.html"
      to: "triply/src/app/shared/components/error-banner/error-banner.component.ts"
      via: "template usage of <app-error-banner>"
      pattern: "app-error-banner"
---

<objective>
Wire per-source error banners, upgrade button prominence, and apply consistent card styling to the Transport, Tours, and Attractions search components.

Purpose: These 3 components (the second half of the 6 search categories) get the same error banner, button prominence, and consistency treatment as the first 3 in plan 11-03. Runs in parallel with 11-03.
Output: Updated TS, HTML, and SCSS for transport-search, tour-search, and attraction-search components.
</objective>

<execution_context>
@C:/Users/Pichau/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Pichau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ux-polish/11-RESEARCH.md
@.planning/phases/11-ux-polish/11-01-SUMMARY.md
@.planning/phases/11-ux-polish/11-02-SUMMARY.md
@triply/src/app/features/transport-search/transport-search.component.ts
@triply/src/app/features/transport-search/transport-search.component.html
@triply/src/app/features/transport-search/transport-search.component.scss
@triply/src/app/features/tour-search/tour-search.component.ts
@triply/src/app/features/tour-search/tour-search.component.html
@triply/src/app/features/tour-search/tour-search.component.scss
@triply/src/app/features/attraction-search/attraction-search.component.ts
@triply/src/app/features/attraction-search/attraction-search.component.html
@triply/src/app/features/attraction-search/attraction-search.component.scss
@triply/src/app/shared/components/error-banner/error-banner.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error banners and button prominence to Transport search</name>
  <files>
    triply/src/app/features/transport-search/transport-search.component.ts
    triply/src/app/features/transport-search/transport-search.component.html
  </files>
  <action>
**transport-search.component.ts changes:**
1. Import ErrorBannerComponent from '../../shared/components/error-banner/error-banner.component'
2. Add ErrorBannerComponent to the imports array
3. Add signals: `errorMessage = signal<string | null>(null)` and `errorSource = signal<string | null>(null)`
4. Add `dismissError()` method: sets both to null
5. In searchTransport() method:
   - Add `this.errorMessage.set(null)` at start
   - In subscribe next, when result.error: set errorMessage/errorSource from result.error instead of snackbar
   - Remove snackbar.open from error path (keep only itinerary success snackbar)

**transport-search.component.html changes:**
1. Add error banner after search form card, before results section:
```html
@if (errorMessage()) {
  <app-error-banner
    [source]="errorSource() ?? 'Transport'"
    [message]="errorMessage()!"
    (dismiss)="dismissError()"
  />
}
```
2. Change `<button mat-button color="primary" (click)="addToItinerary(route)">` to `<button mat-raised-button color="primary" (click)="addToItinerary(route)">`
  </action>
  <verify>Run `npx ng build --configuration=development 2>&1 | head -30`. Build succeeds. Grep transport-search.component.html for "app-error-banner" and "mat-raised-button".</verify>
  <done>Transport search shows persistent error banner on API failure, "Add to Itinerary" uses mat-raised-button.</done>
</task>

<task type="auto">
  <name>Task 2: Add error banners and button prominence to Tours and Attractions search</name>
  <files>
    triply/src/app/features/tour-search/tour-search.component.ts
    triply/src/app/features/tour-search/tour-search.component.html
    triply/src/app/features/attraction-search/attraction-search.component.ts
    triply/src/app/features/attraction-search/attraction-search.component.html
  </files>
  <action>
Apply the SAME error banner pattern to tour-search and attraction-search:

**For EACH component (tour-search and attraction-search):**

**TS file changes:**
1. Import ErrorBannerComponent from '../../shared/components/error-banner/error-banner.component'
2. Add ErrorBannerComponent to the imports array
3. Add `errorMessage = signal<string | null>(null)` and `errorSource = signal<string | null>(null)` signals
4. Add `dismissError()` method
5. In the search method (searchTours / searchAttractions):
   - Add `this.errorMessage.set(null)` at start
   - In subscribe next, when result.error: set errorMessage/errorSource instead of snackbar
   - Remove snackbar.open from error path (keep only itinerary success snackbar)

**HTML file changes:**
1. Add error banner after search form card, before results section. Use source names 'Tours' and 'Attractions' respectively.
2. Change `mat-button` to `mat-raised-button` on "Add to Itinerary" buttons:
   - tour-search: `<button mat-button color="primary" (click)="addToItinerary(tour)">` → `<button mat-raised-button color="primary" (click)="addToItinerary(tour)">`
   - attraction-search: `<button mat-button color="primary" (click)="addToItinerary(attraction)">` → `<button mat-raised-button color="primary" (click)="addToItinerary(attraction)">`
  </action>
  <verify>Run `npx ng build --configuration=development 2>&1 | head -30`. Build succeeds. Grep tour-search.component.html and attraction-search.component.html for "app-error-banner" and "mat-raised-button".</verify>
  <done>Tours and Attractions search pages show persistent error banners on API failure and use mat-raised-button for primary action.</done>
</task>

</tasks>

<verification>
1. `npx ng build --configuration=development` completes without errors
2. All 3 components (transport-search, tour-search, attraction-search) have errorMessage and errorSource signals
3. All 3 HTML templates contain `<app-error-banner>` element
4. All 3 HTML templates use `mat-raised-button` for "Add to Itinerary" buttons
5. No snackbar.open calls remain for error handling in these 3 components
</verification>

<success_criteria>
Transport, Tours, and Attractions search pages display per-source error banners on API failure (not transient snackbars), and all "Add to Itinerary" buttons use mat-raised-button color="primary" for visual prominence.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-polish/11-04-SUMMARY.md`
</output>
